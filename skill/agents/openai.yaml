interface:
  display_name: "Webflow Data API CLI"
  short_description: "Read or change Webflow data with approved access"
  default_prompt: "Use $wf-cli to inspect or change the named Webflow site through the wf command after a person grants access, then prove the result with a fresh read-back."
