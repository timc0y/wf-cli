interface:
  display_name: "Webflow Data API"
  short_description: "Read or change Webflow data with approved access"
  default_prompt: "Use $wf to read or change the named Webflow site after a person grants access, then read the result back."
